# secure-ssh.sh
# author lilynelsen25
# creates new ssh user using $1 parameter
# adds a public key from the local repo or curled from the remote rep.
# removes root ability to ssh
echo "ALL YOUR CODE GOES HERE"
